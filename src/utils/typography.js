import Typography from "typography";

const typography = new Typography({
  baseFontSize: "16px",
  baseLineHeight: 1.45,
  headerFontFamily: ["Merriweather", "serif"],
  bodyFontFamily: ["Kadwa", "serif"]
  // See below for the full list of options.
});

export default typography;
